services:
  rocky10build:
    build:
      context: .
      args:
        rocky: $RL10
    image: "aursu/php85build:${RL10TAG}-build"
  rocky9build:
    build:
      context: .
      args:
        rocky: $RL9
    image: "aursu/php85build:${RL9TAG}-build"
